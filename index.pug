include includes/_files.pug

doctype html

html( lang="en" )
	include includes/head.pug

	body

		a( href="PATTERNS/CC0/" ) CC0 &rarr;

		ul
			each image in images
				li
					a( href="WMDEMO/" + image )
						img( src="WMDEMO/" + image )
